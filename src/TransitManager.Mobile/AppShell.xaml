<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TransitManager.Mobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:TransitManager.Mobile.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="TransitManager.Mobile">

    <!-- L'application s'ouvrira sur cet onglet -->
    <TabBar>
        <ShellContent
            Title="Clients"
            Icon="user_icon.png"
            ContentTemplate="{DataTemplate local:ClientsPage}"
            Route="ClientsPage" />

        <!-- AJOUTER CETTE SECTION -->
        <ShellContent
            Title="Colis"
            Icon="box_icon.png"
            ContentTemplate="{DataTemplate local:ColisPage}"
            Route="ColisPage" />
			
        <ShellContent
            Title="Véhicules"
            Icon="car_icon.png"
            ContentTemplate="{DataTemplate local:VehiculesPage}"
            Route="VehiculesPage" />
    </TabBar>

</Shell>