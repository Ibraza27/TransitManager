<Window x:Class="TransitManager.WPF.Views.AddColisToConteneurView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="700" Width="1100"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Barre de recherche -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Rechercher (code-barres, référence, client, destination...)" 
                     materialDesign:TextFieldAssist.HasClearButton="True" materialDesign:TextFieldAssist.LeadingIcon="Magnify" 
                     Style="{StaticResource MaterialDesignOutlinedTextBox}" 
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Column="1" Style="{StaticResource MaterialDesignIconButton}" Command="{Binding RefreshCommand}" ToolTip="Actualiser" Margin="10,0,0,0">
                <materialDesign:PackIcon Kind="Refresh"/>
            </Button>
        </Grid>

        <!-- Liste des colis non affectés -->
        <DataGrid Grid.Row="1" Margin="0,16,0,0" ItemsSource="{Binding UnassignedColis}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  SelectionMode="Extended"
                  xmlns:mah="clr-namespace:TransitManager.WPF.Helpers"
                  Style="{StaticResource MaterialDesignDataGrid}">
            <DataGrid.Resources>
                <Style TargetType="DataGridRow">
                    <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Window}}"/>
                    <Setter Property="ContextMenu">
                        <Setter.Value>
                            <ContextMenu>
                                <MenuItem Header="Ajouter ce colis" Command="{Binding PlacementTarget.Tag.AddSingleColisCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}">
                                    <MenuItem.Icon><materialDesign:PackIcon Kind="Plus" Foreground="Green"/></MenuItem.Icon>
                                </MenuItem>
                                <Separator/>
                                <MenuItem Header="Ouvrir l'inventaire" Command="{Binding PlacementTarget.Tag.ViewInventaireCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}">
                                    <MenuItem.Icon><materialDesign:PackIcon Kind="ClipboardList"/></MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="Voir détails du colis" Command="{Binding PlacementTarget.Tag.ViewColisDetailsCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}">
                                    <MenuItem.Icon><materialDesign:PackIcon Kind="PackageVariant" /></MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="Voir détails du client" Command="{Binding PlacementTarget.Tag.ViewClientDetailsCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}">
                                    <MenuItem.Icon><materialDesign:PackIcon Kind="AccountEye" /></MenuItem.Icon>
                                </MenuItem>
                                <Separator/>
                                <MenuItem Header="Actualiser" Command="{Binding PlacementTarget.Tag.RefreshCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                    <MenuItem.Icon><materialDesign:PackIcon Kind="Refresh" /></MenuItem.Icon>
                                </MenuItem>
                            </ContextMenu>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.Resources>
            
			<DataGrid.Columns>
				<!-- Colonne pour la case à cocher -->
				<DataGridTemplateColumn Width="Auto">
					<DataGridTemplateColumn.Header>
						<!-- Optionnel : une case pour tout cocher/décocher -->
						<CheckBox/>
					</DataGridTemplateColumn.Header>
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>

				<!-- Les colonnes de données pointent maintenant vers Colis.Propriété -->
				<DataGridTextColumn Header="Codes-barres" Binding="{Binding Colis.AllBarcodes}" Width="140" />
				<DataGridTextColumn Header="Client" Binding="{Binding Colis.Client.NomComplet}" Width="*" />
				<DataGridTextColumn Header="Désignation" Binding="{Binding Colis.Designation}" Width="2*" />
				<DataGridTextColumn Header="Destination" Binding="{Binding Colis.DestinationFinale}" Width="150" />
				<DataGridTextColumn Header="Pièces" Binding="{Binding Colis.NombrePieces}" Width="80" />
				
				<!-- COLONNES FINANCIÈRES AJOUTÉES AVEC LES COULEURS -->
				<DataGridTextColumn Header="Prix Total" Binding="{Binding Colis.PrixTotal, StringFormat=C}" Width="100">
					<DataGridTextColumn.ElementStyle>
						<Style TargetType="TextBlock"><Setter Property="FontWeight" Value="Bold"/><Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/></Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>

				<DataGridTextColumn Header="Total Payé" Binding="{Binding Colis.SommePayee, StringFormat=C}" Width="100">
					<DataGridTextColumn.ElementStyle>
						<Style TargetType="TextBlock"><Setter Property="FontWeight" Value="Bold"/><Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/></Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>

				<DataGridTextColumn Header="Total Restant" Binding="{Binding Colis.RestantAPayer, StringFormat=C}" Width="100">
					<DataGridTextColumn.ElementStyle>
						<Style TargetType="TextBlock"><Setter Property="FontWeight" Value="Bold"/><Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/></Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>

				<DataGridTemplateColumn Header="Statut" Width="120">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<Border Background="{Binding Colis.Statut, Converter={StaticResource StatusToColorConverter}}" CornerRadius="12" Padding="8,4">
								<TextBlock Text="{Binding Colis.Statut}" Foreground="White" FontSize="11" HorizontalAlignment="Center"/>
							</Border>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
        </DataGrid>

        <!-- Boutons d'action -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Ajouter la sélection" Style="{StaticResource MaterialDesignRaisedButton}" Command="{Binding AddSelectedCommand}" Margin="0,0,10,0" Background="{StaticResource SuccessBrush}"/>
            <Button Content="Fermer" Style="{StaticResource MaterialDesignOutlinedButton}" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>