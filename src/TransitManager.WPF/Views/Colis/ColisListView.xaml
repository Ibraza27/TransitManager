<UserControl x:Class="TransitManager.WPF.Views.Colis.ColisListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <materialDesign:Card Grid.Row="0" Padding="16" Margin="16,16,16,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Gestion des Colis / Marchandises" Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                    <TextBlock Text="{Binding StatusMessage}" Opacity="0.7" Margin="0,5,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource MaterialDesignRaisedButton}" Command="{Binding NewColisCommand}" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <!-- CORRECTION ICI: PackagePlus -> PackageVariantPlus -->
                            <materialDesign:PackIcon Kind="PackageVariantPlus" Margin="0,0,5,0"/>
                            <TextBlock Text="Nouveau Colis"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" Command="{Binding ExportCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FileExcel" Margin="0,0,5,0"/>
                            <TextBlock Text="Exporter"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Barre de recherche et filtres (inchangée) -->
        <materialDesign:Card Grid.Row="1" Margin="16,8,16,0" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MaxWidth="500"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Rechercher (code-barres, référence, client...)" materialDesign:TextFieldAssist.HasClearButton="True" materialDesign:TextFieldAssist.LeadingIcon="Magnify" Style="{StaticResource MaterialDesignOutlinedTextBox}" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Return" Command="{Binding SearchCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10,0,0,0">
                        <Button Style="{StaticResource MaterialDesignIconButton}" Command="{Binding RefreshCommand}" ToolTip="Actualiser">
                            <materialDesign:PackIcon Kind="Refresh"/>
                        </Button>
                        <ToggleButton Style="{StaticResource MaterialDesignSwitchToggleButton}" IsChecked="{Binding ShowFilters}" ToolTip="Afficher/Masquer les filtres" Margin="5,0,0,0">
                            <materialDesign:PackIcon Kind="FilterVariant"/>
                        </ToggleButton>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="1" Margin="0,10,0,0" Visibility="{Binding ShowFilters, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Column="0" materialDesign:HintAssist.Hint="Statut" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,0,5,0" SelectedItem="{Binding SelectedStatut}" ItemsSource="{Binding StatutsList}"/>
                    <ComboBox Grid.Column="1" materialDesign:HintAssist.Hint="Client" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="5,0" IsEditable="True" ItemsSource="{Binding ClientsList}" SelectedItem="{Binding SelectedClient}" DisplayMemberPath="NomComplet"/>
                    <ComboBox Grid.Column="2" materialDesign:HintAssist.Hint="Conteneur" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="5,0" ItemsSource="{Binding ConteneursList}" SelectedItem="{Binding SelectedConteneur}" DisplayMemberPath="NumeroDossier}"/>
                    <DatePicker Grid.Column="3" materialDesign:HintAssist.Hint="Date d'arrivée" Style="{StaticResource MaterialDesignOutlinedDatePicker}" Margin="5,0,0,0" SelectedDate="{Binding SelectedDate}"/>
                    <Button Grid.Column="4" Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding ClearFiltersCommand}" Margin="10,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FilterRemove" Margin="0,0,5,0"/>
                            <TextBlock Text="Effacer"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Grid>
        </materialDesign:Card>

        <!-- Liste des colis -->
        <materialDesign:Card Grid.Row="2" Margin="16,8,16,8">
            <DataGrid ItemsSource="{Binding Colis}" SelectedItem="{Binding SelectedClient}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Style="{StaticResource MaterialDesignDataGrid}">
				<DataGrid.Columns>
					<!-- Code-barres (affiche le premier ou "N/A") -->
					<DataGridTextColumn Header="Code-barres" Binding="{Binding Barcodes[0].Value, TargetNullValue='N/A'}" Width="140" />
					
					<!-- Client -->
					<DataGridTextColumn Header="Client" Binding="{Binding Client.NomComplet}" Width="*" />

					<!-- Désignation -->
					<DataGridTextColumn Header="Désignation" Binding="{Binding Designation}" Width="2*" />
					
					<!-- Destination -->
					<DataGridTextColumn Header="Destination" Binding="{Binding DestinationFinale}" Width="150" />
					
					<!-- Nombre de Pièces -->
					<DataGridTextColumn Header="Pièces" Binding="{Binding NombrePieces}" Width="80" />

					<!-- Type de Colis -->
					<DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100" />
					
					<!-- Valeur Déclarée -->
					<DataGridTextColumn Header="Valeur" Binding="{Binding ValeurDeclaree, StringFormat=C}" Width="100" />
					
					<!-- Prix Total -->
					<DataGridTextColumn Header="Prix Total" Binding="{Binding PrixTotal, StringFormat=C}" Width="100">
						<DataGridTextColumn.ElementStyle>
							<Style TargetType="TextBlock">
								<Setter Property="FontWeight" Value="Bold"/>
							</Style>
						</DataGridTextColumn.ElementStyle>
					</DataGridTextColumn>

					<!-- Statut -->
					<DataGridTemplateColumn Header="Statut" Width="120">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<Border Background="{Binding Statut, Converter={StaticResource StatusToColorConverter}}" CornerRadius="12" Padding="8,4">
									<TextBlock Text="{Binding Statut}" Foreground="White" FontSize="11" HorizontalAlignment="Center"/>
								</Border>
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>
				</DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>

        <!-- Barre de statut / Statistiques -->
        <materialDesign:Card Grid.Row="3" Margin="16,0,16,16" Padding="8">
            <!-- Contenu de la barre de statut (inchangé) -->
             <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <WrapPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="8,0">
                        <materialDesign:PackIcon Kind="Package" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBlock VerticalAlignment="Center" Text="{Binding TotalColis, StringFormat='Total : {0} colis'}"/>
                    </StackPanel>
                    <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
                    <StackPanel Orientation="Horizontal" Margin="8,0">
                        <materialDesign:PackIcon Kind="Weight" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBlock VerticalAlignment="Center" Text="{Binding PoidsTotal, StringFormat='Poids total : {0:N0} kg'}"/>
                    </StackPanel>
                    <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
                    <StackPanel Orientation="Horizontal" Margin="8,0">
                        <materialDesign:PackIcon Kind="CubeOutline" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBlock VerticalAlignment="Center" Text="{Binding VolumeTotal, StringFormat='Volume total : {0:N2} m³'}"/>
                    </StackPanel>
                </WrapPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Indicateur de chargement -->
        <Grid Grid.Row="0" Grid.RowSpan="4" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" Background="#80000000">
            <materialDesign:Card HorizontalAlignment="Center" VerticalAlignment="Center" Padding="20">
                <StackPanel>
                    <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" Value="0" IsIndeterminate="True" Width="50" Height="50"/>
                    <TextBlock Text="Chargement..." Margin="0,10,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>