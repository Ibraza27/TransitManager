<UserControl x:Class="TransitManager.WPF.Views.Conteneurs.ConteneurListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <materialDesign:Card Grid.Row="0" Padding="16" Margin="16,16,16,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Gestion des Conteneurs / Dossiers" Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource MaterialDesignRaisedButton}" Command="{Binding NewConteneurCommand}" Background="{StaticResource SuccessBrush}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="PlusBox" Margin="0,0,5,0"/>
                            <TextBlock Text="Nouveau Dossier"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Filtres -->
        <materialDesign:Card Grid.Row="1" Margin="16,8,16,0" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MaxWidth="500"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0"
                       materialDesign:HintAssist.Hint="Rechercher (N° conteneur, plomb, compagnie, destination...)"
                       materialDesign:TextFieldAssist.HasClearButton="True"
                       materialDesign:TextFieldAssist.LeadingIcon="Magnify"
                       Style="{StaticResource MaterialDesignOutlinedTextBox}"
                       Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                
                <Button Grid.Column="1" Style="{StaticResource MaterialDesignIconButton}" Command="{Binding RefreshCommand}" Margin="10,0,0,0" ToolTip="Actualiser">
                    <materialDesign:PackIcon Kind="Refresh"/>
                </Button>
            </Grid>
        </materialDesign:Card>

        <!-- Liste des conteneurs -->
        <materialDesign:Card Grid.Row="2" Margin="16,8,16,16">
            <DataGrid ItemsSource="{Binding Conteneurs}"
                      SelectedItem="{Binding SelectedConteneur}"
                      AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      Style="{StaticResource MaterialDesignDataGrid}">
                
                <DataGrid.Resources>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                        <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_MouseDoubleClick"/>
                    </Style>
                </DataGrid.Resources>
                
                <DataGrid.Columns>
                    <!-- CHANGEMENT 1 : Renommage de la colonne -->
                    <DataGridTextColumn Header="N° Conteneur" Binding="{Binding NumeroDossier}" Width="Auto" FontWeight="Bold"/>

                    <!-- NOUVELLE COLONNE : Statut -->
                    <DataGridTemplateColumn Header="Statut" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Statut, Converter={StaticResource StatusToColorConverter}}"
                                        CornerRadius="12"
                                        Padding="12,4">
                                    <TextBlock Text="{Binding Statut}" 
                                               Foreground="White"
                                               FontSize="11"
                                               HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="N° Plomb" Binding="{Binding NumeroPlomb}" Width="Auto"/>
                    <DataGridTextColumn Header="Compagnie" Binding="{Binding NomCompagnie}" Width="*"/>
                    <DataGridTextColumn Header="Destination" Binding="{Binding Destination}" Width="*"/>
                    <DataGridTextColumn Header="Transitaire" Binding="{Binding NomTransitaire}" Width="*"/>
                    <DataGridTextColumn Header="Date Réception" Binding="{Binding DateReception, StringFormat='dd/MM/yy'}" Width="Auto"/>
                    <DataGridTextColumn Header="Date Chargement" Binding="{Binding DateChargement, StringFormat='dd/MM/yy'}" Width="Auto"/>
                    <DataGridTextColumn Header="Date Départ" Binding="{Binding DateDepart, StringFormat='dd/MM/yy'}" Width="Auto"/>
                    <DataGridTextColumn Header="Date Arrivée" Binding="{Binding DateArriveeDestination, StringFormat='dd/MM/yy'}" Width="Auto"/>
					<DataGridTextColumn Header="Date Clôture" Binding="{Binding DateCloture, StringFormat='dd/MM/yy'}" Width="Auto"/>
                    <DataGridTextColumn Header="Commentaires" Binding="{Binding Commentaires}" Width="2*"/>
                    
                    <DataGridTemplateColumn Header="Actions" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                            Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Modifier / Voir Détails">
                                        <materialDesign:PackIcon Kind="Pencil" />
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Supprimer">
                                        <materialDesign:PackIcon Kind="Delete" Foreground="{StaticResource ErrorBrush}" />
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>

        <!-- Indicateur de chargement -->
        <Grid Grid.Row="0" Grid.RowSpan="3"
              Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
              Background="#80FFFFFF">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" Value="0" IsIndeterminate="True" Width="50" Height="50"/>
        </Grid>
    </Grid>
</UserControl>