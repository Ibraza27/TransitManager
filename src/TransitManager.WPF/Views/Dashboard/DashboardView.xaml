<UserControl x:Class="TransitManager.WPF.Views.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- En-tête -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="{Binding Title}" Style="{StaticResource MaterialDesignHeadline5TextBlock}" VerticalAlignment="Center"/>
                <Button Command="{Binding RefreshCommand}" Style="{StaticResource MaterialDesignIconButton}" HorizontalAlignment="Right" ToolTip="Actualiser les données">
                    <materialDesign:PackIcon Kind="Refresh" />
                </Button>
            </Grid>

            <!-- Cartes de statistiques -->
            <ItemsControl Grid.Row="1" ItemsSource="{Binding Alertes}" Margin="0,0,0,16">
                <!-- Mettez ici le code pour afficher les alertes si nécessaire -->
            </ItemsControl>
            
            <UniformGrid Grid.Row="2" Columns="4" Margin="0,0,0,16">
                <!-- Ajoutez ici les cartes de statistiques (Total Clients, Nouveaux Clients, etc.) -->
            </UniformGrid>

            <!-- Graphiques et listes -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!-- Colonne de gauche (Graphiques) -->
                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                    <materialDesign:Card Padding="16" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Chiffre d'affaires des 12 derniers mois" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,10"/>
                            <lvc:CartesianChart Series="{Binding SeriesChiffreAffaires}" XAxes="{Binding AxesX}" YAxes="{Binding AxesY}" Height="250"/>
                        </StackPanel>
                    </materialDesign:Card>

                    <materialDesign:Card Padding="16">
                        <StackPanel>
                            <TextBlock Text="Évolution des clients" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,10"/>
                            <lvc:CartesianChart Series="{Binding SeriesEvolutionClients}" Height="250"/>
                        </StackPanel>
                    </materialDesign:Card>
                </StackPanel>

                <!-- Colonne de droite (Listes) -->
                <StackPanel Grid.Column="1" Margin="8,0,0,0">
                    <materialDesign:Card Padding="16" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Répartition des colis" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,10"/>
                            <lvc:PieChart Series="{Binding SeriesRepartitionColis}" Height="200"/>
                        </StackPanel>
                    </materialDesign:Card>

                    <materialDesign:Card Padding="16">
                        <StackPanel>
                            <TextBlock Text="Clients Récents" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,10"/>
                            <DataGrid ItemsSource="{Binding DerniersClients}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="None" CanUserAddRows="False">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Binding="{Binding NomComplet}" Width="*"/>
                                    <DataGridTextColumn Binding="{Binding DateInscription, StringFormat='dd/MM/yy'}" Width="Auto"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </materialDesign:Card>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>