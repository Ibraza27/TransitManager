<Window x:Class="TransitManager.WPF.Views.PrintPreviewView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
		xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
		xmlns:helpers="clr-namespace:TransitManager.WPF.Helpers"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="800" Width="600"
        WindowStartupLocation="CenterOwner"
        Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Afficheur de PDF -->
        <wv2:WebView2 x:Name="PdfBrowser" Source="{Binding PdfSource, Mode=OneWay}" Margin="10"/>

        <!-- Barre d'options et de commandes -->
        <materialDesign:Card Grid.Row="1" Padding="16" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0" materialDesign:HintAssist.Hint="Sélectionner une imprimante"
                          ItemsSource="{Binding AvailablePrinters}"
                          SelectedItem="{Binding SelectedPrinter}"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          Margin="0,0,10,0"/>

                <TextBox Grid.Column="1" materialDesign:HintAssist.Hint="Copies"
                         Text="{Binding NumberOfCopies, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Width="80" Margin="0,0,10,0"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="Imprimer" Command="{Binding PrintCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,10,0"/>
                    <Button Content="Fermer" IsCancel="True" Style="{StaticResource MaterialDesignOutlinedButton}"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Indicateur de chargement -->
        <Grid Grid.RowSpan="2" Background="#80FFFFFF" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" IsIndeterminate="True" Width="50" Height="50"/>
        </Grid>
    </Grid>
</Window>