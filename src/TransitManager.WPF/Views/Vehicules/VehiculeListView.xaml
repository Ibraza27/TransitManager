<UserControl x:Class="TransitManager.WPF.Views.Vehicules.VehiculeListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
			 xmlns:converters="clr-namespace:TransitManager.WPF.Converters" 			 
             d:DesignHeight="600" d:DesignWidth="1200">

    <UserControl.Resources>
        <converters:ToUpperConverter x:Key="ToUpperConverter" />
    </UserControl.Resources>
	
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <materialDesign:Card Grid.Row="0" Padding="16" Margin="16,16,16,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Gestion des Véhicules" Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource MaterialDesignRaisedButton}" Command="{Binding NewVehiculeCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Car" Margin="0,0,5,0"/>
                            <TextBlock Text="Nouveau Véhicule"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Barre de recherche et filtres -->
        <materialDesign:Card Grid.Row="1" Margin="16,8,16,0" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MaxWidth="500"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Rechercher (immatriculation, client, conteneur...)" 
                             materialDesign:TextFieldAssist.HasClearButton="True" 
                             materialDesign:TextFieldAssist.LeadingIcon="Magnify" 
                             Style="{StaticResource MaterialDesignOutlinedTextBox}" 
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10,0,0,0">
                        <Button Style="{StaticResource MaterialDesignIconButton}" Command="{Binding RefreshCommand}" ToolTip="Actualiser">
                            <materialDesign:PackIcon Kind="Refresh"/>
                        </Button>
                        <ToggleButton Style="{StaticResource MaterialDesignSwitchToggleButton}" IsChecked="{Binding ShowFilters}" ToolTip="Afficher/Masquer les filtres" Margin="5,0,0,0">
                            <materialDesign:PackIcon Kind="FilterVariant"/>
                        </ToggleButton>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="1" Margin="0,10,0,0" Visibility="{Binding ShowFilters, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/> <!-- Colonne ajoutée -->
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Column="0" materialDesign:HintAssist.Hint="Statut" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,0,5,0" SelectedItem="{Binding SelectedStatut}" ItemsSource="{Binding StatutsList}"/>
                    <ComboBox Grid.Column="1" materialDesign:HintAssist.Hint="Client" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="5,0" IsEditable="True" ItemsSource="{Binding ClientsList}" SelectedItem="{Binding SelectedClient}" DisplayMemberPath="NomComplet"/>
                    <ComboBox Grid.Column="2" materialDesign:HintAssist.Hint="Conteneur" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="5,0" IsEditable="True" ItemsSource="{Binding ConteneursList}" SelectedItem="{Binding SelectedConteneur}" DisplayMemberPath="NumeroDossier"/>
                    <!-- DatePicker ajouté -->
                    <DatePicker Grid.Column="3" materialDesign:HintAssist.Hint="Date de création" Style="{StaticResource MaterialDesignOutlinedDatePicker}" Margin="5,0,0,0" SelectedDate="{Binding SelectedDate}"/>
                    <Button Grid.Column="4" Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding ClearFiltersCommand}" Margin="10,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FilterRemove" Margin="0,0,5,0"/>
                            <TextBlock Text="Effacer"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Grid>
        </materialDesign:Card>
        
        <!-- ... reste du fichier inchangé ... -->
        <materialDesign:Card Grid.Row="2" Margin="16,8,16,8">
            <DataGrid ItemsSource="{Binding Vehicules}" SelectedItem="{Binding SelectedVehicule}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Style="{StaticResource MaterialDesignDataGrid}">
				<DataGrid.Resources>
					<Style TargetType="DataGridRow">
						<Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
						<Setter Property="ContextMenu">
							<Setter.Value>
								<ContextMenu>
									<MenuItem Header="Modifier / Voir Détails"
											  Command="{Binding PlacementTarget.Tag.EditCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
											  CommandParameter="{Binding}">
										<MenuItem.Icon><materialDesign:PackIcon Kind="Pencil" /></MenuItem.Icon>
									</MenuItem>
									<MenuItem Header="Gérer les Paiements" Command="{Binding PlacementTarget.Tag.OpenPaiementsWindowCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}">
										<MenuItem.Icon><materialDesign:PackIcon Kind="CreditCard" /></MenuItem.Icon>
									</MenuItem>
									<MenuItem Header="Voir détails du client..."
											  Command="{Binding PlacementTarget.Tag.ViewClientDetailsInWindowCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
											  CommandParameter="{Binding}">
										<MenuItem.Icon><materialDesign:PackIcon Kind="AccountEye" /></MenuItem.Icon>
									</MenuItem>
									<Separator/>
									<MenuItem Header="Supprimer"
											  Command="{Binding PlacementTarget.Tag.DeleteCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
											  CommandParameter="{Binding}">
										<MenuItem.Icon><materialDesign:PackIcon Kind="Delete" Foreground="Red" /></MenuItem.Icon>
									</MenuItem>
									<Separator/>
									<MenuItem Header="Actualiser la liste"
											  Command="{Binding PlacementTarget.Tag.RefreshCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
										 <MenuItem.Icon><materialDesign:PackIcon Kind="Refresh" /></MenuItem.Icon>
									</MenuItem>
								</ContextMenu>
							</Setter.Value>
						</Setter>
					</Style>
				</DataGrid.Resources>                
				<DataGrid.Columns>
                    <DataGridTextColumn Header="Immatriculation" Binding="{Binding Immatriculation}" Width="1.5*" FontWeight="Bold"/>
                    <DataGridTextColumn Header="Client" Binding="{Binding Client.NomComplet}" Width="2*"/>
                    <DataGridTextColumn Header="Marque" Binding="{Binding Marque}" Width="*"/>
                    <DataGridTextColumn Header="Modèle" Binding="{Binding Modele}" Width="*"/>
                    <DataGridTextColumn Header="Année" Binding="{Binding Annee}" Width="Auto"/>
                    <DataGridTextColumn Header="Conteneur" Binding="{Binding Conteneur.NumeroDossier}" Width="*"/>
					<DataGridTemplateColumn Header="Commentaires" Width="2*">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding Commentaires, Converter={StaticResource ToUpperConverter}}"
										   Foreground="{StaticResource ErrorBrush}"
										   FontWeight="Medium"
										   TextWrapping="Wrap" 
										   VerticalAlignment="Center"
										   Margin="12,0"/> <!-- Ajoute une marge interne pour l'alignement -->
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>
					<DataGridTemplateColumn Header="Statut" Width="120">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<Border Background="{Binding Statut, Converter={StaticResource StatusToColorConverter}}" CornerRadius="12" Padding="12,4">
									<TextBlock Text="{Binding Statut}" Foreground="White" FontSize="11" HorizontalAlignment="Center"/>
								</Border>
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>
					<DataGridTextColumn Header="Prix Total" Binding="{Binding PrixTotal, StringFormat='{}{0:N2} €'}" Width="*">
						<DataGridTextColumn.ElementStyle>
							<Style TargetType="TextBlock"><Setter Property="FontWeight" Value="Bold"/><Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/></Style>
						</DataGridTextColumn.ElementStyle>
					</DataGridTextColumn>
					<DataGridTextColumn Header="Total Payé" Binding="{Binding SommePayee, StringFormat='{}{0:N2} €'}" Width="*">
						<DataGridTextColumn.ElementStyle>
							<Style TargetType="TextBlock"><Setter Property="FontWeight" Value="Bold"/><Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/></Style>
						</DataGridTextColumn.ElementStyle>
					</DataGridTextColumn>
					<DataGridTextColumn Header="Total Restant" Binding="{Binding RestantAPayer, StringFormat='{}{0:N2} €'}" Width="*">
						<DataGridTextColumn.ElementStyle>
							<Style TargetType="TextBlock"><Setter Property="FontWeight" Value="Bold"/><Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/></Style>
						</DataGridTextColumn.ElementStyle>
					</DataGridTextColumn>
                    <DataGridTemplateColumn Header="Actions" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource MaterialDesignIconButton}" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" ToolTip="Modifier">
                                        <materialDesign:PackIcon Kind="Pencil" />
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" ToolTip="Supprimer">
                                        <materialDesign:PackIcon Kind="Delete" Foreground="{StaticResource ErrorBrush}" />
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
        
        <materialDesign:Card Grid.Row="3" Margin="16,0,16,16" Padding="8">
            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
			
				<StackPanel Orientation="Horizontal" Margin="8,0">
					<materialDesign:PackIcon Kind="CarMultiple" VerticalAlignment="Center" Margin="0,0,5,0"/>
					<TextBlock VerticalAlignment="Center">
						<Run Text="Total : "/>
						<Run Text="{Binding TotalVehicules}" FontWeight="Bold"/>
						<Run Text=" véhicules"/>
					</TextBlock>
				</StackPanel>
				<Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />			
			
                <StackPanel Orientation="Horizontal" Margin="8,0">
                    <materialDesign:PackIcon Kind="CashMultiple" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <TextBlock VerticalAlignment="Center" Text="{Binding PrixTotalGlobal, StringFormat='Prix Total : {0:N2} €'}"/>
                </StackPanel>
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
                <StackPanel Orientation="Horizontal" Margin="8,0">
                    <materialDesign:PackIcon Kind="CashCheck" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{StaticResource SuccessBrush}"/>
                    <TextBlock VerticalAlignment="Center" Text="{Binding TotalPayeGlobal, StringFormat='Total Payé : {0:N2} €'}"/>
                </StackPanel>
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
                <StackPanel Orientation="Horizontal" Margin="8,0">
                    <materialDesign:PackIcon Kind="CashRemove" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{StaticResource ErrorBrush}"/>
                    <TextBlock VerticalAlignment="Center" Text="{Binding TotalRestantGlobal, StringFormat='Total Restant : {0:N2} €'}"/>
                </StackPanel>
            </WrapPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>