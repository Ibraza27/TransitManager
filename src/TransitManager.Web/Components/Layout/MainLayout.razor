
@inherits LayoutComponentBase
@inject CustomAuthenticationStateProvider AuthStateProvider
@inject NavigationManager NavigationManager

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="top-row px-4">
            <AuthorizeView>
                <Authorized Context="AuthContext">
                    <div class="d-flex align-items-center">
                        <span class="me-2">👋 Bonjour, @AuthContext.User.Identity?.Name!</span>
                        
                        @* === DÉBUT MODIFICATION LOGOUT === *@
                        <form action="/account/logout" method="post">
                            <AntiforgeryToken />
                            <button type="submit" class="btn btn-outline-secondary btn-sm">🚪 Déconnexion</button>
                        </form>

                    </div>
                </Authorized>
                <NotAuthorized>
                    <a href="/login" class="btn btn-primary btn-sm">🔑 Connexion</a>
                </NotAuthorized>
            </AuthorizeView>
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

@code {
    [Inject] private IApiService ApiService { get; set; } = default!;

    private async Task Logout()
    {
        Console.WriteLine("🏗️ [MainLayout] 🚪 Tentative de déconnexion...");
        
        // === DÉBUT DE LA MODIFICATION ===
        // 1. Appeler l'API pour supprimer le cookie
        await ApiService.LogoutAsync();

        // 2. Nettoyer l'état côté client (localStorage et état Blazor)
        await AuthStateProvider.MarkUserAsLoggedOut();
        
        // 3. Rediriger
        NavigationManager.NavigateTo("/login", forceLoad: true);
        // === FIN DE LA MODIFICATION ===
    }
}