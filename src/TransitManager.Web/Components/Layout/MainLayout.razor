
@inherits LayoutComponentBase
@inject CustomAuthenticationStateProvider AuthStateProvider
@inject NavigationManager NavigationManager

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

	<main>
        <div class="top-row px-4 d-flex justify-content-between align-items-center">
            
            <!-- AJOUT DU TITRE ENTREPRISE -->
            <div class="d-flex align-items-center">
                <h3 class="mb-0 fw-bold text-primary" style="letter-spacing: 1px;">HIPPOCAMPE IMPORT-EXPORT</h3>
            </div>

            <!-- Section Utilisateur (existante, mais alignée à droite) -->
            <AuthorizeView>
                <Authorized Context="AuthContext">
                    <div class="d-flex align-items-center">
                        <span class="me-3 fw-medium">👋 Bonjour, @AuthContext.User.Identity?.Name!</span>
                        <form action="/account/logout" method="post" class="d-inline">
                            <AntiforgeryToken />
                            <button type="submit" class="btn btn-outline-danger btn-sm rounded-pill px-3">
                                🚪 Déconnexion
                            </button>
                        </form>
                    </div>
                </Authorized>
                <NotAuthorized>
                    <a href="/login" class="btn btn-primary btn-sm rounded-pill px-3">🔑 Connexion</a>
                </NotAuthorized>
            </AuthorizeView>
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

@code {
    [Inject] private IApiService ApiService { get; set; } = default!;

    private async Task Logout()
    {
        Console.WriteLine("🏗️ [MainLayout] 🚪 Tentative de déconnexion...");
        
        // === DÉBUT DE LA MODIFICATION ===
        // 1. Appeler l'API pour supprimer le cookie
        await ApiService.LogoutAsync();

        // 2. Nettoyer l'état côté client (localStorage et état Blazor)
        await AuthStateProvider.MarkUserAsLoggedOut();
        
        // 3. Rediriger
        NavigationManager.NavigateTo("/login", forceLoad: true);
        // === FIN DE LA MODIFICATION ===
    }
}