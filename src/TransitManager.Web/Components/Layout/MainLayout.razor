@inherits LayoutComponentBase
@inject CustomAuthenticationStateProvider AuthStateProvider
@inject NavigationManager NavigationManager

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="top-row px-3 px-md-4 d-flex justify-content-between align-items-center">
            
            <!-- TITRE ENTREPRISE RESPONSIVE -->
            <div class="d-flex align-items-center overflow-hidden">
                <!-- Version Mobile (Petit écran) : Titre court -->
                <h5 class="d-block d-md-none mb-0 fw-bold text-primary text-truncate">
                    HIPPOCAMPE IMPORT-EXPORT
                </h5>
                <!-- Version PC (Écran moyen+) : Titre complet -->
                <h3 class="d-none d-md-block mb-0 fw-bold text-primary" style="letter-spacing: 1px;">
                    HIPPOCAMPE IMPORT-EXPORT
                </h3>
            </div>

            <!-- SECTION UTILISATEUR RESPONSIVE -->
			<AuthorizeView>
				<Authorized Context="AuthContext">
					<div class="d-flex align-items-center gap-2">
						<!-- Texte "Bonjour" caché sur mobile (d-none d-md-inline) -->
						<span class="d-none d-md-inline fw-medium text-secondary">
							👋 @AuthContext.User.Identity?.Name
						</span>
						
						<form action="/account/logout" method="post" class="d-inline">
							<AntiforgeryToken />
							<!-- Bouton Mobile : Avec émoji -->
							<button type="submit" class="btn btn-outline-danger btn-sm d-md-none" title="Déconnexion">
								🚪
							</button>
							<!-- Bouton PC : Texte complet -->
							<button type="submit" class="btn btn-outline-danger btn-sm rounded-pill px-3 d-none d-md-inline-block">
								🚪 Déconnexion
							</button>
						</form>
					</div>
				</Authorized>
				<NotAuthorized>
					<a href="/login" class="btn btn-primary btn-sm rounded-pill px-3">Connexion</a>
				</NotAuthorized>
			</AuthorizeView>
        </div>

        <article class="content px-3 px-md-4">
            @Body
        </article>
    </main>
</div>

@code {
    [Inject] private IApiService ApiService { get; set; } = default!;

    private async Task Logout()
    {
        await ApiService.LogoutAsync();
        await AuthStateProvider.MarkUserAsLoggedOut();
        NavigationManager.NavigateTo("/login", forceLoad: true);
    }
}